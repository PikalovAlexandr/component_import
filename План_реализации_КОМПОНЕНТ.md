# ПЛАН РЕАЛИЗАЦИИ
# Система «КОМПОНЕНТ» — управление библиотеками, закупками и запасами ЭК для KiCad

Оценки даны в человеко-неделях (чн) для команды: 1 разработчик PHP/Symfony (доработка Part-DB), 1 разработчик Python (KiCad-модуль), библиотекарь-эксперт (частичная занятость, домен и приёмка). При одном универсальном разработчике сроки складываются последовательно.

---

## Этап 0. Подготовка и прототип инфраструктуры — 2–3 чн

Работы: развёртывание тестового сервера (Linux, Docker Compose: Part-DB + PostgreSQL + Gitea); включение штатной KiCad HTTP library Part-DB и проверка на KiCad 8/9; включение провайдера Octopart/Nexar (получение ключей API); настройка бэкапов.
Параллельно (библиотекарь): фиксация контракта полей eskd-templates v2.1 по примеру `example` (таблица «поле → свойство KiCad → графа документа»); сбор примеров наименований с приёмкой «5» по 3 классам компонентов.
**Веха М0:** сквозная цепочка «деталь, заведённая вручную в Part-DB → символ в схеме KiCad → поля в netlist» работает.
Риск-контроль: если HTTP library Part-DB не покрывает нужные поля/структуру — зафиксировать список доработок до начала этапа 2.

## Этап 1. Мастер импорта и валидаторы (ПС-2, ядро) — 6–8 чн

Работы (Python): парсеры .kicad_sym/.kicad_mod (развитие прототипа из пилота); валидаторы В-1…В-6, В-9; группировка в детали и слияние приёмки; маппинг полей; автоисправления (кодировка, ×25.4) с протоколом; CLI + отчёт в HTML/MD; загрузка результатов в Part-DB через REST API; генератор примитивных 3D-моделей (WRL: цилиндр/параллелепипед по габаритам) и подстановка (model ...) в футпринты.
Работы (PHP): сущность PartAcceptance (приёмка: код, наименование, ОКП) и её отражение в API/UI; статусы ЖЦ с правилом «разрешён только после валидации».
**Веха М1 (= ПИ-1, ПИ-2):** серия К50-35 импортирована начисто: 65 деталей, все дефекты пилота найдены/исправлены.
Результат для бизнеса: можно начинать массовый перенос унаследованных библиотек силами библиотекаря.

## Этап 2. Двойное УГО и контур библиотекаря — 5–7 чн  → **MVP**

Работы (PHP): сущность Model3D и привязка к футпринтам, каталог ${COMPONENT_3D}; роли символов ГОСТ/IEC у детали; два представления HTTP library («-ГОСТ»/«-IEC»); маршрут согласования (черновик → проверка → выпуск) с назначением проверяющего; журнал версий детали.
Работы (Python): валидатор В-1 (пары УГО) в веб-отчёте; утилита переключения проекта ГОСТ↔IEC; аудит проекта (FR-7).
Работы (совместно): экспорт полей под eskd-templates, прогон реального перечня элементов.
**Веха М2 (= ПИ-3, ПИ-4): MVP.** Разработчики полностью работают из базы: выбор детали, два УГО, статусы, ПЭ одной операцией. Система введена в опытную эксплуатацию для ролей «библиотекарь» и «разработчик».

## Этап 3. Закупки, цены, склад с приёмкой — 5–6 чн

Работы (PHP): остатки/предложения/партии в разрезе приёмки (доработка складского контура Part-DB); заказы поставщикам из потребности; минимальные остатки и уведомления; настройка провайдера Octopart для импортной номенклатуры.
Работы (Python): импортёр прайсов (внутренний формат + 1-й конвертер дистрибьютора, запоминание соответствий, устаревание цен).
**Веха М3 (= ПИ-6, ПИ-7):** закупщик и кладовщик работают в системе; цены отечественной номенклатуры ведутся через прайсы.

## Этап 4. Калькуляция стоимости изделия — 3–4 чн

Работы (Python): парсер .kicad_sch и табличных BOM; сопоставление с базой (код/MPN/наименование + ручное разрешение с запоминанием); расчёт тиража с MOQ и ценовыми порогами; деление «склад/закупка»; версии калькуляций, экспорт XLSX/PDF; веб-форма загрузки.
**Веха М4 (= ПИ-5):** разработчик получает стоимость изделия по загруженной схеме ≤ 5 мин (Ц-4).

## Этап 5. Производство, эксплуатация, документация — 4–6 чн

Работы: комплектация/резервы/списание, комплектовочная ведомость, штрих-коды (на базе штатных механизмов Part-DB); инвентаризация; отчёты (дефицит, EOL/NRND-мониторинг); руководства по разделу 6 ТЗ; программа и методика испытаний; приёмочные испытания ПИ-1…ПИ-8; обучение ролей.
**Веха М5:** ввод в промышленную эксплуатацию.

---

## Сводный график

| Этап | Содержание | Оценка, чн | Веха |
|---|---|---|---|
| 0 | Инфраструктура, контракт полей | 2–3 | М0 |
| 1 | Импорт + валидаторы, 3D-заглушки | 6–8 | М1 |
| 2 | Двойное УГО, согласование, 3D-каталог | 6–8 | **М2 = MVP** |
| 3 | Закупки/склад/приёмка/прайсы | 5–6 | М3 |
| 4 | Калькуляция | 3–4 | М4 |
| 5 | Производство, приёмка системы | 4–6 | М5 |
| **Итого** | | **26–36 чн** | |

Для команды из двух разработчиков (PHP + Python работают параллельно) календарная длительность ориентировочно **4–5 месяцев** до М5, MVP (М2) — **~2 месяца**.

Массовый перенос библиотек — отдельный поток работы библиотекаря, начинается после М1 и идёт параллельно этапам 2–5 (мастер импорта снижает трудоёмкость до просмотра отчётов и разрешения конфликтов).

## Организация работ

- Итерации 1–2 недели, демонстрация на живых данных предприятия в конце каждой.
- Репозитории: форк Part-DB; kicad-module (Python); infra (compose, бэкапы); конвертеры прайсов.
- Тестовые данные: эталонный набор из ТЗ п.5 под управлением версий; CI: юнит-тесты парсеров/валидаторов + автопрогон ПИ-1/ПИ-2 на каждый коммит.
- Правило для форка Part-DB: доработки — отдельными модулями/бандлами, чтобы сохранить возможность подтягивать обновления апстрима.

## Риски и меры

| Риск | Вероятность/влияние | Мера |
|---|---|---|
| HTTP library Part-DB не отдаёт нужные поля/два УГО в требуемом виде | средняя / высокое | Проверка на этапе 0 (М0); запасной вариант — собственный эндпоинт HTTP library в Python-сервисе поверх той же БД |
| Расхождение контракта полей с реальным поведением eskd-templates v2.1 | средняя / среднее | Прогон настоящего ПЭ уже на этапе 0–2 (ПИ-3), контракт — конфигурация, а не код |
| Отрыв форка от апстрима Part-DB | высокая / среднее | Доработки модулями; ежеквартальный merge апстрима; минимизация правок ядра |
| Octopart/Nexar: лимиты/платность API | средняя / низкое | Кэширование, обновление по расписанию; ручной ввод и прайсы как основной канал для отечественной номенклатуры (О-7) |
| Качество распознавания таблиц ТУ для В-7 | высокая / среднее | Таблицы вносятся/подтверждаются библиотекарем (полуавтомат: черновик из PDF + ручная проверка), как зафиксировано в ОП п.5 |
| Один разработчик вместо двух | — | Последовательность этапов сохраняется; MVP смещается к ~3–4 месяцам |
| Сопротивление переходу (привычка к файловым библиотекам) | средняя / высокое | Опытная эксплуатация с М2 на одном реальном проекте; библиотеки-«снимки» для старых проектов (FR-6) |

## Критерии успеха проекта

Соответствуют целям Ц-1…Ц-5 ТЗ и подтверждаются приёмочными испытаниями ПИ-1…ПИ-8. Ключевой показатель внедрения: через 3 месяца после М2 — 100% новых схем ведутся из базы; через 6 месяцев — перенесено ≥ 80% активно используемой номенклатуры.
