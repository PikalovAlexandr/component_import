# Изменения

## v0.7.0 — подготовка боевой выгрузки
- Идемпотентность: `component-import --upload` проверяет существующие
  категории/детали по имени (GET /api/...) и пропускает их — повторный запуск
  не плодит дубли; итог «создано/пропущено» в выводе.
- `component-price push`: выгрузка offers.json в Part-DB штатными сущностями
  (suppliers → orderdetails → pricedetails) — цены работают на живой базе до
  появления Д-1 (PartAcceptance); ограничение: цены приёмки «5» до Д-1
  привязываются к той же детали (различаются артикулом поставщика).
- CLI-тесты (test_cli_smoke.py): контракт аргументов всех 4 утилит;
  покрытие обвязок 0% → 84–92%, total 72% → 89%.

## v0.6.1
- Выгрузка в Part-DB: контрактные поля ЕСКД (Тип, Документ, Примечание, Группа,
  Класс точности, Исключён из ПЭ, Масса, Высота) уходят в parameters детали —
  KiCad HTTP-API отдаёт их в fields карточки (подтверждено на стенде М0).
- Маппинг полей (включая составной) применяется и к payload'ам выгрузки.

## v0.6.0 (итоги массового прогона 66 библиотек PCAD)
- В-5: агрегация «дюймы как мм» — одна запись на футпринт вместо записи на площадку.
- В-2: пропуск символов питания (#PWR / флаг power).
- Режим «библиотека только посадочных мест» (без ложных «сирот»).
- Составной маппинг полей «Вид+Раздел -> Тип»; нормализация кодировки в составных значениях.
- Правило статуса «Сданы в архив» -> «Архив» (аудит проекта запрещает применение).

## v0.5.x
- 0.5.1: фикс сборки (packages в pyproject), проверка установки всех CLI.
- 0.5.0: калькулятор стоимости изделия (component-cost): склад/закупка,
  MOQ, ценовые пороги, приёмка проекта, версии калькуляций (ПИ-5).

## v0.4.0
- Импорт прайс-листов (component-price): внутренний формат, конвертеры,
  память соответствий, устаревание цен (ПИ-6).

## v0.3.0
- В-7: сверка деталей с эталонными таблицами документов ТУ (эталон ОЖ0.464.214
  в комплекте); интеграция в component-import --tu.

## v0.2.0
- Двойное УГО: пары ГОСТ/IEC, правило В-1, представления -GOST/-IEC,
  переключение проекта и аудит FR-7 (component-project) (ПИ-3/ПИ-4).

## v0.1.x
- Мастер импорта PCAD-библиотек: валидаторы В-2…В-6, В-9, слияние приёмки,
  автоисправления с протоколом, 3D-заглушки, отчёт, dry-run выгрузка (ПИ-1/ПИ-2).
