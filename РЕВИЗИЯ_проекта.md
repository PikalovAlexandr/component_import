# РЕВИЗИЯ ПРОЕКТА «КОМПОНЕНТ» — соответствие заданию, проблемы, предложения

> Дата: 2026-08-19. Проверено: репозиторий `component_import` (v0.9.0, 26 тестов,
> ruff чист, теги v0.7.0…v0.9.0), стенд (Part-DB :90 + Gitea :3000 + PostgreSQL),
> документация (План_реализации, README этапов 0/1, CHANGELOG, отчёты интеграции,
> СВОДКА прогона). Все утверждения ниже проверены на живом стенде в ходе ревизии.

## 1. Резюме

Техническая реализация **практически завершена и соответствует плану**: этапы 0–4
закрыты полностью, из этапа 5 закрыты бэкапы (ПИ-8). Python-сторона — 6 CLI,
26 тестов, полный цикл «импорт → валидация → исправление → выгрузка → роли УГО →
вложения ТУ/SPICE». PHP-сторона — все пять доработок Д-1…Д-5 на живом Part-DB
2.13.1. Полный корпус 66 библиотек PCAD залит: 14 896 деталей, 14 985 приёмок,
14 897 ролей УГО, 70 143 параметра ЕСКД.

Открыты **операционные** пункты (ключи Nexar, cron бэкапа, UI-хвосты,
перепрогон корпуса) и найдены **три уязвимости стенда**, одна из которых
сыграла в ходе самой ревизии (очистка /tmp уронила Part-DB — восстановлено,
данные целы благодаря docker-томам).

## 2. Соответствие плану реализации (этапы и вехи)

| Этап/веха | Требование плана | Статус | Подтверждение |
|---|---|---|---|
| 0 / **М0** | Стенд Part-DB+PostgreSQL+Gitea; KiCad HTTP library; контракт полей eskd; бэкапы | **5 из 6 пунктов** | п.1 деталь в KiCad ✅ (ПИ-1 стенда М0); п.2 кириллица ✅; п.3 контракт `config/контракт_полей_eskd.csv` ✅; п.5 backup+restore ✅ (ПИ-8: 308→308 1:1, повторно мигрированная схема); п.6 таблица покрытия ✅ + эмпирически по KiCadHelper. **п.4 Octopart/Nexar — открыт (нет ключей)** |
| 1 / **М1** (ПИ-1, ПИ-2) | Мастер импорта, валидаторы В-1…В-6/В-9, слияние приёмки, автоисправления, 3D-заглушки, К50-35 начисто | ✅ | Golden-тесты (130→65, точные количества дефектов, 0 ошибок после фикса, 12 заглушек); полный корпус exit 0 |
| 2 / **М2 = MVP** (ПИ-3, ПИ-4) | Двойное УГО, представления ГОСТ/IEC, переключение проекта, аудит FR-7, маршрут согласования | ✅ серверно | Д-3: `/{locale}/kicad-api/{gost\|iec}/v1/...` — разные symbolIdStr, фолбэк; Д-4: очередь review с gate_ready (API+HTML); component-project switch — с v0.2.0, ПИ-3 тест |
| 3 / **М3** (ПИ-6, ПИ-7) | Прайсы, устаревание, остатки/цены в разрезе приёмки, заказы | ✅ п.6, **п.7 частично** | ПИ-6: import/confirm/status/push + PHP bulk-upsert ✅. ПИ-7: NOT NULL `part_lots.id_acceptance` (схема не даст создать партию без приёмки) ✅; **UI-форма партии с выбором приёмки и колонки в таблицах остатков — не сделаны (есть сниппет PATCHES.md п.4)** |
| 4 / **М4** (ПИ-5) | Калькулятор: схема/BOM → тираж, MOQ, пороги, склад, версии | ✅ | Тесты ПИ-5 (тиражи 10/100, порог 100, MOQ 50, склад, приёмка «5», версии, вход из схемы) |
| 5 / **М5** | Комплектация, инвентаризация, отчёты, руководства, ПМИ, ПИ-1…ПИ-8, обучение | **~15%** | Закрыто: бэкапы+restore (ПИ-8), cron-рецепты (З-5) в Д5_НАСТРОЙКА.md. Не начато: руководства ролей, ПМИ, формальный прогон всех ПИ, обучение |

## 3. Соответствие по кодам ТЗ

**Функциональные:** ФР-1 (база — источник истины) ✅; ФР-4a ТУ-вложения ✅
(component-attach tu, версионирование git+rev); ФР-4b SPICE ✅ (Sim.* в parameters,
В-8 до выгрузки); ФР-5 видимость в KiCad ✅; ФР-6 «снимки» для старых проектов —
**частично** (атомарные библиотеки сохранены как архив, процедурой обновления
символов не оформлено); ФР-7 аудит проекта ✅ (статусы → error/warning, код 1 для CI);
ФР-8 два УГО ✅.

**Закупки/запасы:** З-3 Octopar/Nexar — **открыт**; З-5 уведомления — рецепт
(cron component-price status + EOL через Manufacturing Status), **не включён**;
З-8 калькулятор ✅; З-9 прайсы ✅.

**Нефункциональные:** НД-1 PostgreSQL + ru_RU.UTF-8 ✅; НД-2 бэкапы ✅ (ротация 30 дн,
restore проверен дважды — до и после миграций); НД-5 кириллица без искажений ✅
(проверено в netlist-логике eskd и в KiCad-API карточках).

**Доработки Part-DB:** Д-1 ✅ (приёмка, процессор `_custom_fields`, bulk-offers,
NOT NULL), Д-2 ✅ (статусы + шлюз выпуска с override), Д-3 ✅ (роли УГО, два
kicad-api), Д-4 ✅ (очередь согласования), Д-5 ✅ (ТУ/SPICE). Гигиена форка:
маркеры `// >>> КОМПОНЕНТ` в 3 местах ядра, PATCHES.md, РАЗВЁРТЫВАНИЕ_ФОРКА.md.

**Риски плана:** Р-1 (HTTP API не покрывает поля/два УГО) — **закрыт**: custom-поля
едут через parameters, два УГО — через Д-3 (sub-request, нулевая зависимость от
KiCadHelper). Р-2 (контракт eskd) — закрыт анализом контракта + поля доезжают до
схемы. Р-3 (отрыв форка) — минимизирован (маркеры, отдельные каталоги).

## 4. Проблемы, найденные в ходе ревизии

### 4.1 Инцидент: стенд жил в /tmp и был уронен очисткой

Весь стек разворачивался в `/tmp/etap0` (вопреки README этапа 0, требующему
`/opt/component`). Очистка /tmp удалила compose-файлы, volume-mount каталог
патчей и токены; контейнер partdb упал (exit 127). **Спасло то, что данные —
в docker-томах** (`etap0_db_data`: схема + 14 896 деталей целы) и то, что архивы
комплектов лежали в репозитории. Восстановление заняло ~15 минут: compose+env из
stage0_kit, файлы Д-1…Д-5 из `PartDB_доработки_Д1-Д5_v1.3.zip`, 3 патча ядра
переприменены по PATCHES.md, токен прочитан из БД (`api_tokens` хранит raw).

Попутно вскрылась ловушка bind-mount: **при отсутствии хост-файла Docker создаёт
каталог с именем файла** (`Part.php` стал каталогом, owned by root, chmod 000) —
это и уронило контейнер при первом рестарте. Чистится только через
`docker run --rm -v /tmp:/t alpine rm -rf ...`.

### 4.2 Регресс: параметры без eda_visibility после полного прогона

`defaultParameterVisibility` настроить через БД не удалось (SettingsBundle берёт
значение только после сохранения через UI), поэтому видимость включалась ручным
`UPDATE parameters SET eda_visibility=true` — но **до** полного прогона (1576 шт.).
Прогон добавил 68 567 параметров уже без видимости → ЕСКД-поля 97% деталей были
скрыты в KiCad. **Исправлено в ходе ревизии** (UPDATE 68 567, проверено на К10-17),
но причина жива: каждый новый выгрузочный прогон будет воспроизводить регресс.

### 4.3 Прочее

- **cron бэкапа не установлен** — скрипты протестированы вручную, но по расписанию
  ничего не крутится (норматив НД-2 фактически не выполняется до установки cron).
- **CI-флейк**: ruff-джоба упала на v0.9.0 без воспроизведения локально, перезапуск
  зелёный — версия ruff не закреплена в workflow.
- **Тег v0.8.2 отсутствует** — исправления по СВОДКЕ вошли в v0.9.0 одним коммитом,
  хотя договаривались тегнуть отдельно.
- **Перепрогон корпуса v0.8.2+ не сделан** — прогноз «остаток 10 692 → ~500–700»
  не подтверждён фактом.
- `APP_DEBUG=1` остался в override на время отладки — убран при восстановлении
  (проверить при переносе в /opt).
- В архиве Д-1…Д-4 v1.2 был мусорный каталог `src2` — артефакт упаковки (в v1.3 чисто).
- Оговорки комплектов из отчётов интеграции (report_hash сериализация — уже закрыт
  в v1.1 SerializedName; UI-хвосты — открыты).

## 5. Предложения

### P0 — устойчивость (сделать до любых новых прогонов)

1. **Перенести стенд в `/opt/component`** (или `~/component`): compose, .env,
   каталог патчей, скрипты. После переноса — контрольный `force-recreate` +
   проверка всех эндпоинтов. Избавляет от повтора инцидента 4.1.
2. **Собрать собственный образ** по РАЗВЁРТЫВАНИЕ_ФОРКА.md:
   `FROM jbtronics/part-db1:2.13.1` (тег, не latest) + COPY ComponentExt + патчи
   ядра на этапе сборки. Убирает volume-override, чек-лист dump-autoload/cache:clear
   и ловушку «файл→каталог». Деплой и откат становятся тегом образа.
3. **Установить cron бэкапа** (`30 2 * * * /opt/component/scripts/backup.sh`) и
   месячный тестовый restore (ПИ-8).
4. **Закрыть причину 4.2 насовсем**: либо включить `defaultParameterVisibility`
   через UI (Настройки → KiCad/EDA), либо добавить `eda_visibility: true` в POST
   `/api/parameters` в `upload_session`/`cli.py` (2 строки на Python-стороне,
   надёжнее — не зависит от настроек инстанса).

### P1 — завершение ТЗ

5. **Перепрогон корпуса** с v0.8.2-фиксами → новая СВОДКА, сверка прогноза
   (особенно C_ATC), обновление плана работ библиотекаря.
6. **Ключи Nexar/Octopart** (закрывает М0 п.4, З-3 и включает EOL/NRND из З-5).
7. **UI-хвосты Д-1/Д-2**: поле приёмки в форме партии (сниппет готов), право
   `@parts.release` нормоконтролёру, ссылка на очередь согласования в меню.
8. **Эмпирика ПИ-7**: создать реальные партии с приёмкой через API/UI,
   проверить раздельные остатки SQL-запросом из README_УСТАНОВКА.
9. **Этап 5 / М5**: руководства четырёх ролей, ПМИ, формальный прогон ПИ-1…ПИ-8
   с протоколом, обучение.

### P2 — качество и гигиена

10. CI: закрепить версии (ruff, pytest) в workflow; добавить джобу `php -l` по
    файлам комплекта — ловит Twig/PHP-опечатки до стенда.
11. Интеграционный smoke-тест инфраструктуры: скрипт «поднял compose → миграции →
    выгрузил golden → проверил kicad-api» — как регресс после инцидентов типа 4.1.
12. Секреты: убрать токены из командных строк в истории (менеджер секретов/env),
    ротировать пароли стенда перед промышленной эксплуатацией.
13. Runbook восстановления (текст п.4.1 — в docs, рядом с backup.sh).
14. Ретро-тег v0.8.2 на коммит исправлений или заметка в CHANGELOG, что вошло в
    v0.9.0 (последнее уже сделано).
15. Device_GOST: прогнать библиотеку через `component-import --no-fix` при
    включении в symbols-репо (гигиена ввода графики).

## 6. Вердикт

Проект **соответствует заданию** в технической части: все пять доработок Part-DB
и все шесть CLI работают на живом стенде и подтверждены приёмочными сценариями
(ПИ-1…ПИ-6, ПИ-8; ПИ-7 — на уровне схемы). До полного закрытия ТЗ остаются
операционные шаги (P1) и три инфраструктурных решения (P0), из которых перенос
стенда из /tmp и собственный образ — обязательны до передачи в промышленную
эксплуатацию. Качество процесса высокое: каждый блок проходил цикл
«комплект → карта расхождений → правки → эталон vNext», все находки стенда
возвращались в комплекты, история — в CHANGELOG и тегах.
