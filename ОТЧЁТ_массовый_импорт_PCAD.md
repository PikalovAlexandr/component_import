# ОТЧЁТ: первый массовый прогон конвейера на реальных данных PCAD

> Веха М0 / «первая живая библиотека после массового импорта».
> Источник: `bufer/PCAD_LIB/Kicad/Kicad_conv` (66 библиотек, 76 МБ).
> Команда: пакетный анализатор `/tmp/batch_pcad.py` → сводка `/tmp/pcad_batch/сводка.json`.
> Режим: валидация + группировка (без записи и выгрузки, только поведение).

## Главный вывод

**Конвейер переварил поток без единого сбоя: 0 падений на 66 библиотеках.**
Все компоненты (символы, футпринты, s-expression) разбираются корректно;
эталон К50-35 в реальных данных свёлся ровно так же, как в golden-тесте
(130 символов → 65 деталей).

## Сводные цифры

| Метрика | Значение |
|---|---|
| Обработано библиотек | 66 (из них 65 с парным `.pretty/`, 1 без — «ПИТАНИЕ») |
| Падений / ошибок разбора | **0** |
| Символов всего | 14 963 |
| Деталей после группировки | 14 896 |
| Футпринтов загружено | 1 160 |
| Деталей с приёмкой (-А) | 154 (только 3 библиотеки ведут суффикс приёмки) |
| Всего находок валидаторов | 57 872 |
| — из них error (блокируют «разрешён») | 56 953 |
| — из них manual (решение библиотекаря) | 800 |

## Структура находок по правилам

| Правило | Уровень | Кол-во | Что значит |
|---|---|---|---|
| В-6 | error | 23 154 | кодировка / обязательные поля |
| В-5 | error | 22 255 | дюймы записаны как мм (детектор ×25.4) — **fixable** |
| В-3 | error | 10 746 | имя вывода ≠ номеру (C/R/L) — **fixable** |
| В-2 | manual | 558 | неклассифицированные площадки |
| В-2 | error | 323 | вывод без площадки |
| В-9 | error | 254 | дубли наименований |
| В-4 | error | 221 | тип выводов ≠ passive (C/R/L) — **fixable** |
| В-3 | manual | 155 | номера вне ряда 1..N |
| В-5 | warning | 119 | мелкие SMD-площадки |
| Л-7 | manual | 87 | детали без базового варианта |

## Аномалии, требующие внимания (пометки для библиотекаря / разработки)

1. **`D_STMICROELECTRONICS` — 18 904 В-5/error.** Микросхемы (класс D), у них
  Thousands-площадки малоразмерные — детектор «дюймы как мм» даёт массовые
   ложные срабатывания. **Доработка:** правило В-5 не должно применяться к SMD-
   площадкам класса D (только к TH-конденсаторам/резисторам). Сейчас шумит.
2. **`ПИТАНИЕ` — 0 футпринтов** (нет парного `.pretty/`). 275 символов без
   посадочных мест → массовые В-2. Нужен отдельный разбор: либо футпринты
   ушли в общий пул, либо их нет.
3. **`A_УЗЛЫ ПЕЧАТНЫЕ` — 0 символов, 53 футпринта.** Библиотека «только
   посадочные места» (платы/узлы) — валидатор В-2/В-3 к ней неприменим,
   но конвейер всё равно прогнал 1096 issues по футпринтам. Нужен фильтр
   «нет символов → не валидируем пин-соответствие».
4. **Приёмка `-А` ведётся в 3 библиотеках из 66** (154 детали). Подавляющее
   большинство библиотек — без слияния приёмки. Это нормально для
   дистрибьюторских/импортных серий (C_GRM, C_ATC, C_AVX…), но значит, что
   `DEFAULT_CONFIG.acceptance_suffixes` достаточно текущего набора.
5. **К10-57/47М/73/17 — массовые В-3 (имя≠номеру) и В-6 (кодировка).** Это
   системная проблема конвертации PCAD→KiCad для отечественных К10-х.
   Автоисправления (fixable) их снимут; после `write_fixed` требуется
   повторный прогон для верификации (как в `cli.py`).

## Что подтверждено (работает корректно)

- Разбор s-expression KiCad для 66 библиотек разной структуры — без сбоев.
- Группировка в детали со слиянием приёмки — эталон К50-35 совпал с golden.
- Валидаторы В-2…В-6, В-9, Л-7, Л-8 работают на потоке, находки
  классифицированы по severity.
- Производительность: 66 библиотек / ~15 000 деталей — за время порядка
  единиц минут (анализ без записи).

## Рекомендации для следующих итераций

1. **Уточнить В-5** под SMD/ИС: массовые ложные срабатывания на D_STMicro.
2. **Добавить фильтр «библиотека без символов»** → пропуск В-2/В-3 (A_УЗЛЫ).
3. Запустить **полный конвейер с записью** (`write_fixed`) на 1–2 больших
   библиотеках (К10-17, К10-73) — проверить автоисправления и верификацию
   «ноль ошибок после fix» в промышленных масштабах.
4. Сверить `field_map_pcad.csv` с контрактом `контракт_полей_eskd.csv`
   (особенно «Ту/Гост-Ту → Документ», «Вид/Раздел → Тип»).
